import { useEffect, useState } from 'react';
import { CheckCircle2, Headset, Send } from 'lucide-react';
import axios from 'axios';
import { toast } from 'sonner';
import { useAuth } from '../context/AuthContext';

const AdminSupport = () => {
    const { token } = useAuth();
    const api = `${import.meta.env.VITE_BACKEND_URL}/api/support`;
    const headers = { headers: { Authorization: `Bearer ${token}` } };
    const [tickets, setTickets] = useState<any[]>([]);
    const [selected, setSelected] = useState<any>(null);
    const [reply, setReply] = useState('');
    const [status, setStatus] = useState('');
    const [saving, setSaving] = useState(false);
    const load = async () => { try { const res = await axios.get(api, headers); if (res.data.success) setTickets(res.data.data || []); } catch { toast.error('Could not load support tickets'); } };
    useEffect(() => { if (token) load(); }, [token]);
    const sendReply = async () => { if (!selected || !reply.trim()) return; setSaving(true); try { const res = await axios.post(`${api}/${selected._id}/messages`, { message: reply }, headers); if (res.data.success) { setSelected(res.data.data); setReply(''); load(); } } catch (error: any) { toast.error(error.response?.data?.message || 'Could not send reply'); } finally { setSaving(false); } };
    const updateStatus = async (next: string) => { if (!selected) return; setStatus(next); try { const res = await axios.patch(`${api}/${selected._id}`, { status: next }, headers); if (res.data.success) { setSelected(res.data.data); load(); toast.success('Ticket status updated'); } } catch { toast.error('Could not update ticket status'); } finally { setStatus(''); } };
    return <div className="space-y-6 animate-in fade-in duration-500"><div><h2 className="text-3xl font-bold mb-2 flex items-center gap-3"><Headset className="text-primary" /> Customer Support</h2><p className="text-muted-foreground">Respond to retailer and distributor questions and track resolution.</p></div><div className="grid grid-cols-1 lg:grid-cols-3 gap-6"><div className="glass-card rounded-2xl border border-border overflow-hidden"><div className="p-4 border-b border-border font-bold">Support requests ({tickets.length})</div><div className="max-h-[650px] overflow-y-auto">{tickets.length === 0 ? <p className="p-6 text-center text-muted-foreground">No support requests.</p> : tickets.map((ticket) => <button key={ticket._id} onClick={() => setSelected(ticket)} className={`w-full text-left p-4 border-b border-border/60 hover:bg-primary/5 ${selected?._id === ticket._id ? 'bg-primary/10 border-l-2 border-l-primary' : ''}`}><div className="flex items-start justify-between gap-2"><p className="font-semibold text-sm truncate">{ticket.subject}</p><span className="text-[10px] font-bold text-muted-foreground">{ticket.status.replace('_', ' ')}</span></div><p className="text-xs text-muted-foreground mt-1">{ticket.userId?.name || ticket.userId?.businessName || 'User'} · {ticket.userModel}</p><p className="text-xs text-muted-foreground mt-2">{new Date(ticket.updatedAt).toLocaleString('en-IN')}</p></button>)}</div></div><div className="lg:col-span-2 glass-card rounded-2xl border border-border min-h-[500px] flex flex-col">{!selected ? <div className="flex-1 flex flex-col items-center justify-center text-center p-8"><Headset className="w-12 h-12 text-primary/50 mb-3" /><h3 className="font-bold text-lg">Select a request</h3><p className="text-sm text-muted-foreground">Choose a ticket to view the conversation and respond.</p></div> : <><div className="p-5 border-b border-border flex flex-wrap items-center justify-between gap-3"><div><h3 className="text-xl font-bold">{selected.subject}</h3><p className="text-sm text-muted-foreground">{selected.userId?.name || selected.userId?.businessName} · {selected.userModel}</p></div><div className="flex items-center gap-2"><select value={selected.status} onChange={(e) => updateStatus(e.target.value)} disabled={Boolean(status)} className="p-2 rounded-lg bg-background border border-border text-sm"><option value="OPEN">Open</option><option value="IN_PROGRESS">In progress</option><option value="RESOLVED">Resolved</option><option value="CLOSED">Closed</option></select>{selected.status === 'RESOLVED' && <CheckCircle2 className="w-5 h-5 text-emerald-500" />}</div></div><div className="flex-1 p-5 space-y-3 overflow-y-auto">{(selected.messages || []).map((item: any, index: number) => <div key={index} className={`flex ${item.senderRole === 'admin' ? 'justify-end' : 'justify-start'}`}><div className={`max-w-[80%] rounded-2xl px-4 py-3 ${item.senderRole === 'admin' ? 'bg-primary text-primary-foreground rounded-br-sm' : 'bg-muted text-foreground rounded-bl-sm'}`}><p className="text-sm whitespace-pre-wrap">{item.message}</p><p className="text-[10px] opacity-70 mt-1">{new Date(item.createdAt).toLocaleString('en-IN')}</p></div></div>)}</div><div className="p-4 border-t border-border flex gap-2"><input value={reply} onChange={(e) => setReply(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && sendReply()} placeholder="Reply to this request..." className="flex-1 p-3 rounded-xl bg-background border border-border outline-none focus:border-primary" /><button onClick={sendReply} disabled={saving || !reply.trim()} className="p-3 bg-primary text-primary-foreground rounded-xl disabled:opacity-50"><Send className="w-4 h-4" /></button></div></>}</div></div></div>;
};

export default AdminSupport;
